# Market Structure Rules (future support)
# Would feed from btengine.features.market_structure.MarketStructureAnalyzer,
# which is an interface-only stub today (no classification logic exists
# yet - see docs/RESEARCH_PLATFORM_ARCHITECTURE.md). Leave disabled until
# that analyzer is implemented.
#
# TODO(owner): no conditions can be meaningfully defined until the
# underlying analyzer exists.

enabled: false
combination_logic: ALL
conditions: []
notes: "Future support - MarketStructureAnalyzer is not yet implemented."
