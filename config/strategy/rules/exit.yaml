# Exit Rules
# Conditions that trigger closing an open position outright. Distinct
# from trade_management.yaml, which adjusts a still-open position rather
# than closing it. See btengine/strategy/rules/exit.py for the schema and
# docs/strategy_spec.md §Exit Rules for notes.
#
# TODO(owner): this category has no conditions yet. Add entries under
# `conditions` and set `enabled: true` once ready (see
# rules/funding_rate.yaml for a worked example of the shape).

enabled: false
combination_logic: ALL
conditions: []
notes: ""
